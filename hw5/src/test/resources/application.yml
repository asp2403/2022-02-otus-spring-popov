spring:
  main:
    allow-circular-references: true
  shell:
    interactive:
      enabled: false
  datasource:
    url: jdbc:h2:mem:books
  sql:
    init:
      mode: always
      data-locations: data.sql

