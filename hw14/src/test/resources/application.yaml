logging:
  level:
    root: INFO

spring:
  datasource:
    url: jdbc:h2:mem:books

  flyway:
    enabled: true

  mongodb:
    embedded:
      version: 3.5.5

  data:
    mongodb:
      port: 0
      database: asp_library_test

