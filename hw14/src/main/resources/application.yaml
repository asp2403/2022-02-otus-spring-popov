logging:
  level:
    root: INFO
spring:
  datasource:
    url: jdbc:h2:mem:books
  h2:
    console:
      enabled: true
  flyway:
    enabled: true
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true
